{
  "name": "omega-genesis-worker",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/convergence.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  }
}
