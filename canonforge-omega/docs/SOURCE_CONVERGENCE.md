# OMEGA source convergence rules

The runtime is grounded in the recovered OMEGA corpus, not in arbitrary rewrites.

Primary source-backed laws currently implemented:

- 20,736 address = Domain × Phase × Regulation × Layer, each 1..12.
- Mode 188 ratio `S = CΩ / (Λ + q + Λq)` with a configurable turn band.
- Dewey opposing operators `011 construct` and `01-1 prune`, integrated into an Omega state decision.
- Deterministic AutoPing traversal.
- Outverse/Inverse pairing and Hourglass/paired-relation architecture as derived mappings, not new empirical primitives.
- 1+6 shell opposite-pair reduction and simplex projection.

Donor policy:

- `atlas_runtime_test_bundle` is the preferred tested API/runtime donor.
- Mode188 v10/v11 are DB-centered sovereign runtime donors.
- `Dewey_Full_PC_Runtime_188` is a deep logic/workstation donor and must be quarantined/tested before import.
- CanonConsole v24/v31/v31r1/v32/v35 are UI/governance/replay donors.
- Hybrid Link bundles are bridge/service donors and do not become state authority by themselves.
- `CanonConsoleOmega_v44_2_Unified` is rejected as authority because the reviewed branch used synthetic/randomized runtime values.
- Nested meta-archives remain untrusted until second-order contents are fully inventoried.

No donor may silently introduce a second canonical state path, synthetic truth, a stale version overlay, or an unbounded execution primitive.
