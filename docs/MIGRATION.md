# Migration

Preserve useful implementations only after binding them to the Genesis packet/proof contracts.

Do not carry forward shadow CΩ/Φ/Λ/q sliders, secondary phase clocks, mutable external “latest” aliases presented as exact timestamped observations, arbitrary-shell Desktop Link behavior, compiled cache artifacts, or donor modules that claim authority from filenames/version chronology.

Migration order: index and hash corpus → classify donors → wrap accepted modules as adapters → route every state mutation through `OmegaRuntime.propose()` → bind all projections to the canonical digest → run acceptance gates → package/deploy.
