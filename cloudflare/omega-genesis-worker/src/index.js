import { DurableObject } from "cloudflare:workers";
import {MODES,MENUS,GATES,ER} from "./catalog.js";
import {clamp,pad,hash,idx,addr,validAddr,validMetrics,arc,gate,calc,digestState,seed,proj,forecast,classify,mode,orchestrate,dest,hav} from "./kernel.js";\nimport {handleExtendedApi} from "./api_ext.js";

const BUILD_ID="omega-genesis-v1.1";

export class OmegaGenesisState extends DurableObject{
 constructor(ctx,env){super(ctx,env);this.ctx=ctx;this.env=env}
 async load(){let s=await this.ctx.storage.get("state");if(!s){s=await seed();await this.ctx.storage.put("state",s);await this.ctx.storage.put("j:"+pad(0),{origin:"SEED",digest:s.digest,packet:s})}return s}
 async list(prefix){return[...(await this.ctx.storage.list({prefix})).values()]}
 async appendJournal(s,origin="COMMIT"){await this.ctx.storage.put("j:"+pad(s.sequence),{origin,digest:s.digest,packet:s})}
 async appendProof(kind,decision,before,after,payload){const rows=await this.list("p:"),seq=rows.length+1,base={sequence:seq,kind,decision,state_before:before,state_after:after,payload,previous_receipt:rows.at(-1)?.digest||null,created_at:new Date().toISOString()},r={...base,digest:await hash(base)};await this.ctx.storage.put("p:"+pad(seq),r);return r}
 async verify(){const js=await this.list("j:"),ps=await this.list("p:"),s=await this.load();let prev=null,lastSeq=null;for(let i=0;i<js.length;i++){const r=js[i],p=r.packet,b={...p};delete b.digest;const d=await hash(b);if(d!==r.digest||p.digest!==r.digest)return{valid:false,reason:"journal_digest_mismatch",failure_at:i+1};if(i&&p.parent_digest!==prev)return{valid:false,reason:"journal_parent_mismatch",failure_at:i+1};if(i&&p.sequence!==lastSeq+1)return{valid:false,reason:"journal_sequence_gap",failure_at:i+1};prev=r.digest;lastSeq=p.sequence}if(prev!==s.digest)return{valid:false,reason:"journal_head_mismatch"};let pr=null;for(let i=0;i<ps.length;i++){const r=ps[i],b={...r};delete b.digest;if(await hash(b)!==r.digest||r.previous_receipt!==pr||r.sequence!==i+1)return{valid:false,reason:"proof_chain_mismatch",failure_at:i+1};pr=r.digest}const states=new Set(js.map(x=>x.digest)),commits=new Set(ps.filter(x=>x.decision==="COMMIT").map(x=>x.state_after));for(const r of ps.filter(x=>x.decision==="COMMIT"))if(!states.has(r.state_before)||!states.has(r.state_after))return{valid:false,reason:"proof_state_reference_missing"};for(const r of js.filter(x=>x.origin==="COMMIT"))if(!commits.has(r.digest))return{valid:false,reason:"journal_commit_missing_proof"};return{valid:true,journal:{valid:true,records:js.length,head:prev},proof:{valid:true,records:ps.length,head:pr},current_digest:s.digest}}
 async fetch(request){const u=new URL(request.url),s=await this.load();if(u.pathname==="/state"){const p=await this.list("p:");return Response.json({state:{...s,state_id:idx(s.address)+1,index0:idx(s.address)},calculus:calc(s.metrics),projection:await proj(s),proof:{valid:true,records:p.length,head:p.at(-1)?.digest||null},replay:await this.verify()})}if(u.pathname==="/proof"){const p=await this.list("p:");return Response.json({verify:{valid:true,records:p.length,head:p.at(-1)?.digest||null},replay:await this.verify(),records:p.slice(-100)})}if(u.pathname==="/replay")return Response.json(await this.verify());if(u.pathname==="/forecast")return Response.json(forecast(s,u.searchParams.get("horizon")||1));if(u.pathname==="/orchestrate")return Response.json(orchestrate(s));if(u.pathname==="/transition"&&request.method==="POST"){const b=await request.json(),a=b.address||s.address,ev=b.evidence_class||"DERIVED",m={...s.metrics,...(b.metrics||{})};if(!validAddr(a)||!validMetrics(m)||!(ev in ER))return Response.json({error:"invalid_transition"},{status:422});const g=gate(m),prior=forecast(s,1);if(ER[ev]>ER[s.evidence_class]&&["OBSERVED","IMPORTED"].includes(ev)){const r=await this.appendProof("TRANSITION","HOLD_EVIDENCE_PROMOTION",s.digest,null,{requested:ev,current:s.evidence_class});return Response.json({committed:false,decision:"HOLD_EVIDENCE_PROMOTION",gate:g,receipt:r})}if(!(g.admission==="ACCEPT"||(b.allow_conditional&&g.admission==="CONDITIONAL"))){const r=await this.appendProof("TRANSITION",g.admission,s.digest,null,{mode:b.mode||"MODE188",gate:g,forecast_prior:prior,futureObservationUsed:false});return Response.json({committed:false,decision:g.admission,gate:g,forecast_prior:prior,receipt:r})}const n=await digestState({...s,address:a,metrics:m,evidence_class:ev,parent_digest:s.digest,sequence:s.sequence+1,created_at:new Date().toISOString(),payload:b.payload||{},motion:{...s.motion,phase:arc(s.motion.phase,a[1]),transition_progress:1}});await this.appendJournal(n);await this.ctx.storage.put("state",n);const r=await this.appendProof("TRANSITION","COMMIT",s.digest,n.digest,{mode:b.mode||"MODE188",gate:g,forecast_prior:prior,futureObservationUsed:false,projection_fingerprint:(await proj(n)).packet_fingerprint});return Response.json({committed:true,decision:"COMMIT",gate:g,forecast_prior:prior,state:{...n,state_id:idx(n.address)+1,index0:idx(n.address)},receipt:r})}return new Response("Not found",{status:404})}
}

const allowedWrite=(req,env)=>Boolean(env.OMEGA_WRITE_TOKEN)&&req.headers.get("X-Omega-Write-Token")===env.OMEGA_WRITE_TOKEN;
const stub=env=>env.OMEGA_STATE.getByName("primary");
function hybrid(b){const allowed=new Set(["READ_FILE","WRITE_OUTPUT","HASH_TREE","INDEX_CORPUS","RUN_VERIFICATION","TRAIN_LOCAL_BOUNDED"]),errors=[];(b.steps||[]).forEach((s,i)=>{if(!allowed.has(s.op))errors.push(`step ${i}: op not allowed: ${s.op}`)});return{status:errors.length?"FAIL":"PASS",errors,executed:false,policy:"cloud validates typed plans only; host execution requires an authorized local node"}}
export default{async fetch(request,env){const u=new URL(request.url);if(u.pathname==="/_omega/health")return Response.json({ok:true,layer:"omega-genesis-cloud",build:env.BUILD_ID||BUILD_ID,authority:"durable-object-canonical",writeConfigured:Boolean(env.OMEGA_WRITE_TOKEN),privateCorpusEmbedded:false});if(!(u.pathname.startsWith("/api/")||u.pathname.startsWith("/host/")))return env.ASSETS.fetch(request);const st=stub(env),state=async()=>await(await st.fetch("https://state/state")).json();const extended=await handleExtendedApi({url:u,request,env,stateStub:st,stateSnapshot:state});if(extended)return extended;if(u.pathname==="/api/health"){const s=await state();return Response.json({status:"OK",runtime:"OMEGA_GENESIS_CLOUD",version:"1.1.0",canonical_digest:s.state.digest,state_id:s.state.state_id,proof:s.proof,replay:s.replay,writeConfigured:Boolean(env.OMEGA_WRITE_TOKEN)})}if(u.pathname==="/api/state")return st.fetch("https://state/state");if(u.pathname==="/api/proof")return st.fetch("https://state/proof");if(u.pathname==="/api/replay")return st.fetch("https://state/replay");if(u.pathname==="/api/forecast")return st.fetch(`https://state/forecast${u.search}`);if(u.pathname==="/api/orchestrate")return st.fetch("https://state/orchestrate");if(u.pathname==="/api/modes")return Response.json({modes:MODES.map(id=>({id,name:id.replaceAll("_"," "),purpose:"Governed Genesis mode",mutation_policy:["MODE188","DEWEY_BAL","GUIDANCE_FIELD","TRUTH_TRAVERSAL","PRUNE_TRANSLATE_PROVE","FORECAST","LANGUAGE"].includes(id)?"PROPOSE":"READ_ONLY",evidence_boundary:"One canonical packet; no silent evidence promotion."}))});if(u.pathname==="/api/capabilities")return Response.json({menus:MENUS,capabilities:Array.from({length:18},(_,i)=>({id:`CAP-${String(i+1).padStart(3,"0")}`,status:"BOUND"})),acceptance_gates:GATES});if(u.pathname==="/api/plugins")return Response.json({policy:"hosted compiled/read-only plugin registry",plugins:[{id:"atlas_echo",name:"Atlas Echo",version:"1.0.0",status:"PASS",permissions:["atlas_query","state_read"],capabilities:["atlas.query","state.read"],mutations:[]}]});if(u.pathname==="/api/plugins/run"&&request.method==="POST"){const b=await request.json(),s=await state();return b.id==="atlas_echo"?Response.json({status:"PASS",returncode:0,stdout:JSON.stringify({plugin:"atlas_echo",status:"PASS",received_state_id:s.state.state_id,note:"hosted read-only adapter; no canonical mutation"}),stderr:""}):Response.json({error:"plugin_not_found"},{status:404})}if(u.pathname==="/api/atlas"){const i=Math.max(0,Math.min(20735,Math.trunc(+(u.searchParams.get("index")||0)))),a=addr(i);return Response.json({index0:i,state_id:i+1,address:a,opposite:a.map(v=>(v+5)%12+1),phase_portal_size:1728})}if(u.pathname==="/api/corpus/classify"){const n=u.searchParams.get("name")||"";return Response.json({name:n,...classify(n)})}if(u.pathname==="/api/mode"){const id=(u.searchParams.get("id")||"MODE188").toUpperCase(),s=await state();return Response.json({mode:id,result:id==="ALL_MODES"?orchestrate(s.state).results.ALL_MODES:MODES.includes(id)?mode(id,s.state):{mode:id,status:"UNKNOWN_MODE"},state_digest:s.state.digest})}if(u.pathname==="/api/dewey-bal/validate"&&request.method==="POST"){const b=await request.json(),score=1-clamp(b.source_burden),checks={source_state:+b.source_state===11499,target_state:+b.target_state===11687,source_burden:Math.abs(+b.source_burden-.8000063837447882)<1e-9,target_burden:Math.abs(+b.target_burden-.42901814817581707)<1e-9,edge:b.edge==="MODE188+",score:Math.abs(score-.19999361625521184)<1e-12};return Response.json({decision:Object.values(checks).every(Boolean)?"ACCEPT":"HOLD",checks,score,required_order:["CHECKPOINT_SOURCE","FREEZE_FORECAST_PRIOR","COMMIT_ADMITTED_EDGE"]})}if(u.pathname==="/api/transition"&&request.method==="POST"){if(!allowedWrite(request,env))return Response.json({error:"hosted_write_locked",boundary:"Configure OMEGA_WRITE_TOKEN and send X-Omega-Write-Token."},{status:403});return st.fetch(new Request("https://state/transition",{method:"POST",headers:{"content-type":"application/json"},body:await request.text()}))}if(u.pathname==="/api/hybrid/validate"&&request.method==="POST")return Response.json(hybrid(await request.json()));if(u.pathname==="/api/hybrid/run"&&request.method==="POST")return Response.json({error:"host_only_operation",boundary:"Hybrid Link execution requires an authorized local OMEGA host node."},{status:409});if((u.pathname==="/api/workbook/inspect"||u.pathname==="/api/workbook/roundtrip")&&request.method==="POST")return Response.json({error:"host_only_operation",boundary:"Workbook operations require an authorized local host."},{status:409});if(u.pathname==="/api/host/status")return Response.json({status:"BOUNDARY",host:"cloudflare-worker",platform:"Cloudflare Workers",hybrid_roots:[],canonical_authority:"Durable Object",boundary:"PC status is local-host only."});if(u.pathname==="/api/release/verify")return Response.json({status:"CI_VERIFIED_BUILD",build:env.BUILD_ID||BUILD_ID,boundary:"Repository hashing runs in CI before deployment."});if(u.pathname==="/api/earth/destination"&&request.method==="POST"){const b=await request.json(),o={lat:+b.lat,lon:+b.lon},br=+b.bearing_rad||0,d=+b.distance_m||0,t=dest(o.lat,o.lon,br,d);return Response.json({origin:o,destination:t,requested_distance_m:d,computed_distance_m:hav(o,t),bearing_rad:br,quantized_heading_rad:Math.round(br/(Math.PI/36))*(Math.PI/36),evidence_class:"DERIVED",boundary:"geodesic computation only; not current ground conditions"})}return Response.json({error:"not_found"},{status:404})}};
