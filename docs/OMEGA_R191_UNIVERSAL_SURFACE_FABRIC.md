# OMEGA R191 — Universal Surface Fabric

R191 is an additive surface-convergence organ over the admitted R190 cumulative system. It does not replace the Living Field, R189 Whole Instrument, R190 capability-truth plane, Genesis, Optical, Sovereign/Hybrid, the 172-cloud fabric, AI/SAI, computation, validation, or any existing Durable Object namespace.

## Cumulative identity

- Protected semantic base identity remains `r87-semantic-edge-settle-proof`.
- R189 remains the immutable 61-group predecessor body: 24 base families + 37 extension organs.
- R190 contributes exactly one capability-truth/admission organ, for 62 groups.
- R191 contributes exactly one new organ: `R191_UNIVERSAL_SURFACE_FABRIC`, for 63 protected capability groups.
- R188 synchronous motion-time remains one specialized execution regime; it is not converted into a universal scheduler.

## New surfaces

Canonical OMEGA V6 exposes:

- `/fabric` — responsive Universal Surface Fabric operator view.
- `/api/fabric/r191/manifest` — read-only surface/authority registry and exact deployment identity.
- `/api/fabric/r191/status` — live service-binding and external-surface observations.
- `/api/canon/r191/manifest` — cumulative 63-group capability Canon.

The existing `/instrument` surface links to `/fabric` without changing R189's 61-group semantic contract.

## Authority law

`GENESIS_PROPOSES -> OPTICAL_SCREENS -> SOVEREIGN_EXECUTES_WHEN_AUTHENTICATED -> VALIDATION_GRADES -> OMEGA_V6_ADMITS`

R191 keeps these states separate:

1. A route may be declared.
2. A surface may be reachable.
3. A service may be writable by its own control plane.
4. A capability may be invoked and return a receipt.
5. Returned evidence may be verified.
6. Only the canonical proof-gated release path can admit operational Canon.

Reachability therefore does not imply write authority, execution proof, validation, or promotion authority.

## Service observations

The canonical Worker uses its existing Cloudflare service bindings to probe:

- `GENESIS` -> `omega-genesis-v1`
- `OMEGA_GENESIS_MACHINE` -> `omega-genesis-machine-r115`
- `OMEGA_OPTICAL_MACHINE` -> `omega-optical-machine-r115`

The machine organs remain role-separated:

- Genesis Machine: `PROPOSE_ONLY`
- Optical Machine: `SCREEN_ONLY`
- OMEGA V6: canonical operational/admission authority

The Sovereign PC is never reported ONLINE merely because a cloud surface is reachable. A current authenticated Sovereign heartbeat remains required.

## Living Light Vercel target

Target URL:

`https://omega-living-light-etching-private-woven2.vercel.app/`

R191 may observe the target URL as an external HTTPS surface, but same-URL promotion remains blocked until the connected Vercel control plane proves project-level write access and the resulting exact deployment is post-deploy probed.

The connected Vercel control plane currently exposes no writable team/project for this target. R191 therefore reports:

- `sameUrlPromotionAuthorized: false`
- blocker: `VERCEL_PROJECT_WRITE_ACCESS_REQUIRED`
- `everywherePromotionProved: false`

This is an intentional truth boundary, not a missing success flag.

A deploy-ready static mirror is included at:

`canonforge-omega/vercel/omega-r191-universal-surface/`

That mirror reads canonical R191 status from OMEGA V6; it does not create another Canon authority.

## Address and dimensional-relativity boundary

R191 carries the software atlas/address/execution hierarchy:

`12 -> 144 -> 1,728 -> 20,736 -> 248,832`

These are software address/execution/presentation resolution levels. They are not literal physical dimensions. Whole/part, inner/outer, and representation remain relative-frame roles.

## Truth boundaries retained

- Visual state is not execution proof.
- Model output is not CanonState.
- Optical scalar/reduced-order screening is not RCWA/FDTD/FEM validation.
- Independent full-wave evidence is not fabrication measurement.
- PC ONLINE requires current authenticated heartbeat evidence.
- A reachable protected Vercel URL is not proof that OMEGA can write or promote that project.
- R191 has `canonicalMutation:false` and `promotionAuthorized:false`.

## Promotion gate

R191 promotion requires the existing full OMEGA V6 proof-gated deployment path plus the R191-specific workflow. The R191 live gate requires:

- exact `CANONICAL_GIT_SHA` equality with the promoted branch head;
- R189 cumulative coverage = 61 and complete;
- R190 cumulative Canon = 62;
- R191 cumulative Canon = 63 with exactly one R191 organ;
- preserved R87/R189/R190 identities and Durable Object namespaces;
- live Genesis Machine and Optical Machine service-binding probes;
- `/fabric`, R191 manifest, R191 status, and R191 cumulative Canon responding from the public canonical Worker;
- protected Vercel target remaining explicitly unpromoted unless its separate write-access gate is actually satisfied.

The build is therefore allowed to advance everywhere that OMEGA actually controls while explicitly identifying externally protected surfaces that still require their own authenticated deployment authority.
