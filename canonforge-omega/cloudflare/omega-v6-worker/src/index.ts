export interface Env {
  SOVEREIGN_ORIGIN?: string;
  SOVEREIGN_GATEWAY_TOKEN?: string;
  BUILD_ID?: string;
}

const html = `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>OMEGA V6 · Sovereign Runtime</title>
<style>
:root{color-scheme:dark;--bg:#06080d;--panel:#0d1119;--line:#273044;--text:#eef2ff;--muted:#9aa8c4;--alpha:#8b5cf6;--base:#e5b94d;--construct:#ef5b5b;--prune:#4b8cff;--omega:#43c782;font:15px/1.45 Inter,system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 50% -10%,#1a2130 0,#0a0d14 36%,#050609 74%);color:var(--text)}header{position:sticky;top:0;z-index:10;display:flex;align-items:center;gap:14px;padding:14px 20px;border-bottom:1px solid var(--line);background:#080b11ee;backdrop-filter:blur(16px)}.brand{font-weight:850;letter-spacing:.13em}.badge{padding:5px 10px;border:1px solid #3c4868;border-radius:999px}.small{font-size:.84rem;color:var(--muted)}.shell{max-width:1500px;margin:auto;padding:18px 22px 40px}.topnav{display:flex;align-items:center;gap:8px;overflow:auto;padding:2px 0 18px}.topnav button,.action,.operator{border:1px solid var(--line);background:#0b1019;color:#dce6ff;border-radius:12px;cursor:pointer}.topnav button{padding:11px 14px;white-space:nowrap}.topnav button.active{background:#182236;border-color:#50658b}.workspace{display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:18px}.canvas,.sidepanel{background:linear-gradient(180deg,#0d121c,#090d14);border:1px solid var(--line);border-radius:20px;box-shadow:0 22px 70px #0008}.canvas{min-height:620px;padding:24px;display:grid;grid-template-rows:auto minmax(340px,1fr) auto;gap:18px}.sidepanel{padding:18px}.titlebar{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.titlebar h1{margin:0;font-size:clamp(1.65rem,3vw,2.5rem)}.stage{position:relative;min-height:360px;border:1px solid #222c40;border-radius:18px;overflow:hidden;background:radial-gradient(circle at center,#121c2b 0,#0b1018 42%,#070a0f 100%)}.orbit{position:absolute;inset:9%;border:1px solid #36445f;border-radius:50%}.orbit:before,.orbit:after{content:"";position:absolute;border:1px dashed #2c3850;border-radius:50%;inset:15%}.orbit:after{inset:31%;border-style:solid}.core{position:absolute;left:50%;top:50%;translate:-50% -50%;width:120px;height:120px;border-radius:50%;display:grid;place-items:center;background:#111a29;border:1px solid #596b8f;font-weight:850;font-size:1.35rem;box-shadow:0 0 45px #415d9350}.operatorRing{position:absolute;inset:0}.operator{position:absolute;width:132px;padding:10px;text-align:left;transform:translate(-50%,-50%);box-shadow:0 8px 30px #0008}.operator strong{display:block}.operator span{display:block;font-size:.75rem;color:#c5cde0}.operator.alpha{left:50%;top:8%;border-color:var(--alpha);box-shadow:0 0 24px color-mix(in srgb,var(--alpha) 30%,transparent)}.operator.base{left:88%;top:33%;border-color:var(--base)}.operator.construct{left:76%;top:82%;border-color:var(--construct)}.operator.prune{left:24%;top:82%;border-color:var(--prune)}.operator.omega{left:12%;top:33%;border-color:var(--omega)}.operator.active{outline:2px solid #fff4;transform:translate(-50%,-50%) scale(1.04)}.legend{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:9px}.role{padding:11px;border:1px solid var(--line);border-radius:12px;background:#0a0f17}.role b{display:block;margin-bottom:3px}.role.alpha b{color:var(--alpha)}.role.base b{color:var(--base)}.role.construct b{color:var(--construct)}.role.prune b{color:var(--prune)}.role.omega b{color:var(--omega)}.metricgrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.metric{padding:12px;border:1px solid #222c40;border-radius:12px;background:#090d14}.value{font-size:1.2rem;font-weight:750}.action{width:100%;padding:11px 12px;margin-top:10px;text-align:left}.action.primary{background:#16233b;border-color:#536d9e}.statusline{display:flex;gap:8px;align-items:center;padding:10px 0}.dot{width:9px;height:9px;border-radius:50%;background:#69758c}.dot.ok{background:var(--omega)}.dot.warn{background:var(--base)}.dot.err{background:var(--construct)}.drawer{margin-top:14px;border-top:1px solid #222c40;padding-top:14px}.drawer summary{cursor:pointer;color:#cfd9ee}.drawer pre{white-space:pre-wrap;word-break:break-word;color:#aebbd5;max-height:260px;overflow:auto}.app{display:none}.app.active{display:block}.hybridSteps{display:grid;gap:9px;margin-top:14px}.step{padding:12px;border:1px solid #273044;border-radius:12px;background:#0a0f17}.step b{display:block}.earthgrid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.earthbox{min-height:160px;padding:14px;border:1px solid #283248;border-radius:14px;background:radial-gradient(circle at 70% 30%,#173041,#0a0e15 58%)}.oktxt{color:#90e6b5}.warntxt{color:#ffd78a}.errtxt{color:#ff9b9b}@media(max-width:980px){.workspace{grid-template-columns:1fr}.sidepanel{order:-1}.canvas{min-height:560px}.legend{grid-template-columns:1fr 1fr}.operator{width:116px;font-size:.82rem}}@media(max-width:640px){header{padding:12px}.shell{padding:12px}.canvas{padding:14px;min-height:520px}.stage{min-height:390px}.operator{width:102px;padding:8px}.operator span{display:none}.legend{grid-template-columns:1fr}.earthgrid{grid-template-columns:1fr}}
</style></head><body>
<header><div class="brand">OMEGA V6</div><span class="badge" id="status">connecting</span><span class="small" id="build"></span></header>
<div class="shell"><nav class="topnav" id="nav"></nav><div class="workspace"><main class="canvas">
<div class="app active" data-app="Field"><div class="titlebar"><div><div class="small">CURRENT LOCATION · COMPUTATION</div><h1>Operator Field</h1><p class="small">ALPHA, BASE, CONSTRUCT, PRUNE and OMEGA are functional operator roles. 144 / 1728 / 20,736 remain representation shells, not physical dimensions.</p></div></div></div>
<div class="app" data-app="Hybrid"><div class="titlebar"><div><div class="small">CURRENT LOCATION · SYSTEM</div><h1>Hybrid Link</h1><p class="small">Browser readiness is not PC connection proof. PC ONLINE requires an authenticated current heartbeat.</p></div></div><div class="hybridSteps" id="hybridSteps"></div></div>
<div class="app" data-app="Earth"><div class="titlebar"><div><div class="small">CURRENT LOCATION · EARTH</div><h1>Earth Evidence</h1><p class="small">Source-backed Earth state and degradation belong here; unavailable evidence stays visibly unavailable.</p></div></div><div class="earthgrid"><div class="earthbox"><b>Observed / source-backed</b><p class="small" id="earthObserved">Awaiting source response.</p></div><div class="earthbox"><b>Model / derived</b><p class="small" id="earthDerived">Separated from observed evidence.</p></div></div></div>
<div class="app" data-app="Proof"><div class="titlebar"><div><div class="small">CURRENT LOCATION · EVIDENCE</div><h1>Proof & Rollback</h1><p class="small">Detailed telemetry is progressive disclosure instead of primary-screen content.</p></div></div></div>
<div class="stage" id="fieldStage"><div class="orbit"></div><div class="operatorRing"><button class="operator alpha" data-role="ALPHA"><strong>ALPHA</strong><span>purple · seed</span></button><button class="operator base" data-role="BASE"><strong>BASE</strong><span>gold · substrate</span></button><button class="operator construct" data-role="CONSTRUCT"><strong>CONSTRUCT</strong><span>red · expansion</span></button><button class="operator prune" data-role="PRUNE"><strong>PRUNE</strong><span>blue · inversion</span></button><button class="operator omega" data-role="OMEGA"><strong>OMEGA</strong><span>green · integration</span></button></div><div class="core" id="core">Ω</div></div>
<div class="legend"><div class="role alpha"><b>ALPHA</b><span class="small">seed / initiation</span></div><div class="role base"><b>BASE</b><span class="small">substrate / support</span></div><div class="role construct"><b>CONSTRUCT</b><span class="small">expansion / build</span></div><div class="role prune"><b>PRUNE</b><span class="small">inversion / removal</span></div><div class="role omega"><b>OMEGA</b><span class="small">integration / synthesis</span></div></div>
</main><aside class="sidepanel"><div class="small">LIVE STATE</div><div class="statusline"><span class="dot" id="stateDot"></span><strong id="stateLabel">Awaiting runtime</strong></div><div class="metricgrid" id="metrics"></div><button class="action primary" id="refreshBtn">Refresh authoritative state</button><button class="action" id="hybridBtn">Open Hybrid connection</button><details class="drawer"><summary>Technical proof</summary><pre id="proof">No proof loaded.</pre></details></aside></div></div>
<script>
const apps=['Field','Earth','Hybrid','Proof'];nav.innerHTML=apps.map((x,i)=>'<button data-appbtn="'+x+'" class="'+(i===0?'active':'')+'">'+x+'</button>').join('');
function openApp(name){document.querySelectorAll('[data-appbtn]').forEach(b=>b.classList.toggle('active',b.dataset.appbtn===name));document.querySelectorAll('.app').forEach(a=>a.classList.toggle('active',a.dataset.app===name));fieldStage.style.display=name==='Field'?'block':'none';document.querySelector('.legend').style.display=name==='Field'?'grid':'none';if(name==='Hybrid')refreshHybrid();if(name==='Earth')refreshEarth();if(name==='Proof')loadProof()}
nav.addEventListener('click',e=>{const b=e.target.closest('[data-appbtn]');if(b)openApp(b.dataset.appbtn)});hybridBtn.onclick=()=>openApp('Hybrid');refreshBtn.onclick=()=>refresh();
function metric(k,v,c=''){return '<div class="metric"><div class="small">'+k+'</div><div class="value '+c+'">'+v+'</div></div>'}
function setRole(role){document.querySelectorAll('.operator').forEach(b=>b.classList.toggle('active',b.dataset.role===role));core.textContent=role.slice(0,1)+'Ω'}document.querySelectorAll('.operator').forEach(b=>b.onclick=()=>setRole(b.dataset.role));
async function loadProof(){try{const p=await fetch('/api/omega/proof?limit=6',{cache:'no-store'});proof.textContent=JSON.stringify(await p.json(),null,2)}catch(e){proof.textContent=String(e)}}
async function refreshHybrid(){hybridSteps.innerHTML='<div class="step"><b>Checking connection contract…</b><span class="small">Credential and heartbeat are evaluated separately.</span></div>';try{const r=await fetch('/api/hybrid/status',{cache:'no-store'});const d=await r.json();const online=Boolean(d.nativeExecutionClaimed||d.pcOnline||d.heartbeatCurrent);const stale=Boolean(d.heartbeatStale);const state=online?'PC ONLINE':stale?'HEARTBEAT STALE':d.browserCredentialReady?'AGENT NOT RUNNING / UNREACHABLE':'BROWSER CREDENTIAL REQUIRED';hybridSteps.innerHTML=['BROWSER CREDENTIAL','WINDOWS AGENT','AUTHENTICATING','HEARTBEAT PROOF','PC CAPABILITY'].map((x,i)=>'<div class="step"><b>'+x+'</b><span class="small">'+(i===0?(d.browserCredentialReady?'READY':'REQUIRED'):i===3?(online?'AUTHENTICATED CURRENT HEARTBEAT':stale?'STALE HEARTBEAT':'NO AUTHENTICATED HEARTBEAT'):i===4?(online?'PC ONLINE':'DEVICE PROOF REQUIRED'):'State governed by authenticated agent contract')+'</span></div>').join('')+'<div class="step"><b>Current result</b><span class="small">'+state+'</span></div>'}catch(e){hybridSteps.innerHTML='<div class="step"><b>AGENT / STATUS UNREACHABLE</b><span class="small">'+String(e)+'</span></div>'}}
async function refreshEarth(){try{const r=await fetch('/api/earth/catalog',{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);const d=await r.json();earthObserved.textContent=(d.catalog||d.coverages||d.status)?'Earth source catalog responded. Evidence remains source-bounded.':'Earth endpoint responded without a recognized catalog.';earthDerived.textContent='Derived/model layers remain separate from observed source state.'}catch(e){earthObserved.textContent='Source unavailable or endpoint degraded: '+String(e);earthDerived.textContent='No fabricated Earth state is substituted.'}}
async function refresh(){try{const r=await fetch('/api/omega/state',{cache:'no-store'});if(!r.ok)throw new Error('host '+r.status);const d=await r.json();status.textContent='sovereign host linked';status.className='badge oktxt';build.textContent='state '+d.digest.slice(0,12);stateDot.className='dot ok';stateLabel.textContent='AUTHORITATIVE STATE AVAILABLE';core.textContent='P'+String(d.state.address[1]).padStart(2,'0');metrics.innerHTML=metric('Mode 188',d.mode188.dispatch,d.mode188.dispatch==='STAY'?'oktxt':'warntxt')+metric('S',Number(d.mode188.ratio).toFixed(4))+metric('Construct',Number(d.operator.construct_011).toFixed(4))+metric('Prune',Number(d.operator.prune_01m1).toFixed(4))+metric('Address',d.state.address.join('·'))+metric('Evidence',d.state.evidence_class);const role=d.mode188.dispatch==='TURN'?'PRUNE':d.mode188.dispatch==='ESCALATE'?'CONSTRUCT':'OMEGA';setRole(role);loadProof()}catch(e){status.textContent='sovereign host unavailable';status.className='badge errtxt';stateDot.className='dot err';stateLabel.textContent='NO AUTHORITATIVE STATE';metrics.innerHTML=metric('Runtime','OFFLINE','errtxt')+metric('Authority','No data fabricated');proof.textContent=String(e)}}refresh();setInterval(refresh,5000);
</script></body></html>`;

function upstream(request: Request, env: Env): Request | null {
  if (!env.SOVEREIGN_ORIGIN || !env.SOVEREIGN_GATEWAY_TOKEN) return null;
  const origin = env.SOVEREIGN_ORIGIN.replace(/\/$/, '');
  const incoming = new URL(request.url);
  const target = new URL(origin + incoming.pathname + incoming.search);
  const headers = new Headers(request.headers);
  headers.delete('cookie');
  headers.delete('authorization');
  headers.set('x-omega-public-gateway', 'cloudflare-worker');
  headers.set('x-omega-gateway-token', env.SOVEREIGN_GATEWAY_TOKEN);
  return new Request(target, { method: request.method, headers, body: request.method === 'GET' || request.method === 'HEAD' ? undefined : request.body, redirect: 'manual' });
}

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    if (url.pathname === '/_omega/health') {
      return Response.json({ ok: true, layer: 'public-interface', build: env.BUILD_ID || 'dev', sovereignConfigured: Boolean(env.SOVEREIGN_ORIGIN), gatewayTokenConfigured: Boolean(env.SOVEREIGN_GATEWAY_TOKEN), authority: 'public-interface-only' });
    }
    if (url.pathname.startsWith('/api/')) {
      const proxy = upstream(request, env);
      if (!proxy) return Response.json({ error: 'sovereign_gateway_not_configured', boundary: 'public gateway requires origin + secret and will not fabricate runtime state' }, { status: 503 });
      try {
        const response = await fetch(proxy);
        const headers = new Headers(response.headers);
        headers.set('cache-control', 'no-store');
        headers.set('x-omega-authority', 'sovereign-runtime');
        return new Response(response.body, { status: response.status, headers });
      } catch (error) {
        return Response.json({ error: 'sovereign_host_unreachable', detail: String(error) }, { status: 502 });
      }
    }
    return new Response(html, { headers: { 'content-type': 'text/html; charset=utf-8', 'cache-control': 'no-cache', 'x-omega-authority': 'public-interface-only' } });
  }
};
